@model  IEnumerable<Tweetinvi.Core.Interfaces.ITweet>

@if (Model.Any())
{
    <div class="tweets">
        <h4>Latest Tweets</h4>
        <ul>
            @foreach (var tweet in Model)
            {
                <li><span class="date">@tweet.CreatedAt.ToString("ddd, d M yyyy"):</span> @tweet.Text</li>
            }
        </ul>
    </div>
}